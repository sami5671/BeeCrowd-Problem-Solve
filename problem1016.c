/*this is the c program for calculating how long it takes (in minutes) for the car Y to take this distance in relation to the other car.*/
#include<stdio.h>
int main(){
int distance,time;
printf("Enter the distance: ");
scanf("%d",&distance);
printf("%d minutos\n",time=distance*2);
return 0;
}
