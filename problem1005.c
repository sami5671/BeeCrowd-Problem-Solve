/*This  is the C program for calculating average number of a student*/
#include <stdio.h>
int main()
{
        float A, B, M;

        // printf("A: ");
        scanf("%f", &A);

        // printf("B: ");
        scanf("%f", &B);

        //   M = ((A * 3.5 + B * 7.5) / (A + B));
        printf("MEDIA = %.5f\n", M = ((A * 3.5 + B * 7.5) / (3.5 + 7.5)));

        return 0;
}
