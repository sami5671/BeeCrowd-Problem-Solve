/*This is the C program for calculating the sum of two number*/
#include <stdio.h>
int main()
{
        int A, B, sum;
        // printf("Enter number A:");
        scanf("%d", &A);

        // printf("Enter number B:");
        scanf("%d", &B);

        printf("X = %d\n", sum = A + B);

        return 0;
}
